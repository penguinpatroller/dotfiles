# Cameron Keybinds
c() { cd "$@" && ls; }

export EDITOR=vim
export VISUAL="$EDITOR"

# Aliases
alias quit="exit"

alias _zshrc="vim ~/.zshrc"
alias _reload_zshrc="source ~/.zshrc"

alias _vimrc="vim ~/.vimrc"

